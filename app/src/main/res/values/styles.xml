<resources>

    <style name="MaterialTheme" parent="ParentTheme">

    </style>

    <!-- Base application theme. -->
    <style name="ParentTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolbarThemeDemo" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="colorControlHighlight">#6b71af</item>

    </style>

    <style name="MenuPopup" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/colorPrimaryDark</item>
        <item name="android:colorBackground">#f2eeee</item>
        <item name="colorControlHighlight">#6b71af</item>
    </style>

</resources>
